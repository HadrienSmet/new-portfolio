import FirstRow from "./FirstRow";
import Form from "./Form";

const Contact = () => {
    return (
        <section className="contact">
            <FirstRow />
            <Form />
        </section>
    );
};

export default Contact;
